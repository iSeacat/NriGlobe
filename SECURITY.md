# 安全政策（Security Policy）

`NriGlobe` 是一个**离线可跑的静态数据可视化工具**：把新闻与社媒内容编译成一颗可交互的情报地球。
本政策说明哪些在范围内、如何报告漏洞，以及数据处理边界。

## 支持版本

| 版本 | 状态 |
| --- | --- |
| `master`（最新） | ✅ 安全更新中 |

我们只在 `master` 上维护安全修复，请基于最新代码提交问题或 PR。

## 报告漏洞

请**不要**通过公开 Issue 报告安全漏洞。

- 联系邮箱：**opensource@iseacat.cn**
- 邮件主题请加前缀 `[SECURITY]`
- 我们会在 **72 小时**内确认收到，并在确认后给出修复时间线。
- 如方便，请附：复现步骤、影响范围、建议修复方式。

## 范围说明

**在本项目范围内：**

- `index.html` 与 `lib/three.min.js` 的前端逻辑
- `scripts/` 下的构建与同步脚本
- `content/` 下的示例源与构建配置

**不在范围内（由使用者自行负责）：**

- 你本地的 `data/dataset.js`、`config.local.json`、`content/markets.local.json` 等**私有文件**——它们**不进仓库**，由你自行保管，不在本政策覆盖之列。
- 你接入的任意外部新闻 / 社媒数据源的账号与凭据。
- 你自行部署的服务器与网络环境。

## 依赖与供应链

- 仓库**内置** `lib/three.min.js`（来自 threejs.org，MIT），不依赖任何在线 CDN，避免供应链劫持与隐私外泄风险。
- 地理数据 `content/countries-110m.json` 来自 world-atlas / Natural Earth（公有领域）。
- 如需升级 three.js，请从官方渠道重新下载并核对许可后再替换该文件，并说明版本来源。

## 隐私

本工具**不收集、不上传**任何运行数据，所有处理均在本地完成。仓库内不含任何真实情报，仅含 `data/dataset.example.js` 纯演示数据。
